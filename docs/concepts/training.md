- Hyperparameters - e.g. learning rate - model parameters (e.g. NN weights) are updated by training, whereas hyperparameters are metavariables which constrain how the model learns.
- Choosing good hyperparameters is often done
via a validation set, a third data split held out from both training and final testing.
- Epochs
